import * as React from "react";
import { useState } from "react";
import { css } from "@emotion/css";
import Layout from "../component/layout";

const GuerillaOA = () => {
  const [thaiText, setThaiText] = useState(false);

  return (
    <Layout></Layout>
  ) 
};

export default GuerillaOA;
